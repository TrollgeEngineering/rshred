# rshred

An interactive, user friendly bash script for recursive shredding.

# How to use

1. Open the terminal.
2. Use `cd` to change your current directory to the one that rshred is located in.
3. Use `chmod u+x rshred` to give your user the ability to execute the script.
4. Run `./rshred`. 